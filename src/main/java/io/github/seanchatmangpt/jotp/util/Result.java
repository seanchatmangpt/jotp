package io.github.seanchatmangpt.jotp.util;

/**
 * Railway-oriented Result<T,E> sealed type.
 *
 * <p>Generated by ggen from RDF ontology. Do not edit — regenerate with {@code ggen sync}.
 */
public sealed interface Result permits Result.Success, Result.Failure {

    /** Successful variant. */
    record Success(String value) implements Result {}

    /** Failure variant. */
    record Failure(String reason) implements Result {}

    // ── Factory methods ──────────────────────────────────────────────────────

    static Result success(String value) {
        return new Success(value);
    }

    static Result failure(String reason) {
        return new Failure(reason);
    }

    // ── Pattern-matching helpers ─────────────────────────────────────────────

    default boolean isSuccess() {
        return this instanceof Success;
    }

    default boolean isFailure() {
        return this instanceof Failure;
    }
}
