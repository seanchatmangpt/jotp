# io.github.seanchatmangpt.jotp.test.LinkCascadeStressTest


ProcLink cascade stress test: chain depth propagation

Tests O(N) cascade propagation through linked chain.



Breaking point under investigation:

- Chain depth: 500 processes (A→B→C→...→N)

- Crash head, measure propagation to tail

- Expected: <10ms per hop (<5s total)

| Key | Value |
| --- | --- |
| `Per-hop latency` | `0.62 ms` |
| `Propagation time` | `309 ms` |
| `Result` | `PASS` |
| `Chain depth` | `500` |
| `Threshold` | `<10ms per hop` |

ProcLink cascade stress test: death star topology

Tests JVM scheduler under 1000 simultaneous thread interrupts.



Breaking point under investigation:

- Hub linked to 1000 workers

- Hub crash triggers 1000 concurrent interrupts

- Tests crash callback loop bottleneck

| Key | Value |
| --- | --- |
| `Interrupts fired` | `1000 concurrent` |
| `Propagation time` | `110 ms` |
| `Dead workers verified` | `100%` |
| `Result` | `PASS` |
| `Workers` | `1000` |

---
*Generated by [DTR](http://www.dtr.org)*
