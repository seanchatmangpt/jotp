# io.github.seanchatmangpt.jotp.distributed.StaticNodeDiscoveryTest


Node removal gracefully decommissions nodes from the cluster.

Removed nodes are excluded from getHealthyNodes() and failover targeting.

This supports planned maintenance and cluster shrinkage.

Node addresses use host:port format for TCP communication.

Supports DNS names, IP addresses, and varying port configurations.

This flexibility accommodates diverse network topologies in production deployments.

Dynamic node registration enables cluster expansion without restart.

New nodes receive HEALTHY status by default and participate in failover.

This supports scaling out distributed OTP applications incrementally.

StaticNodeDiscovery initializes with a fixed cluster topology.

All configured nodes are registered immediately, avoiding dynamic discovery delays.

This suits small clusters with stable membership like Erlang's .hosts.file pattern.

Heartbeat timestamps track node liveness for failure detection.

Nodes exceeding timeout thresholds are marked DOWN or DEGRADED.

This implements OTP's distributed node health monitoring protocol.

Concurrent node registration must be thread-safe in distributed environments.

Multiple nodes may join simultaneously during cluster bootstrap or scaling events.

The backend ensures atomic registration without race conditions.

---
*Generated by [DTR](http://www.dtr.org)*
