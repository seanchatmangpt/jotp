# io.github.seanchatmangpt.jotp.distributed.FailoverControllerTest


Only healthy nodes can accept migrated processes.

When no healthy nodes exist, migration cannot proceed.

Nodes marked DOWN or DEGRADED are excluded from failover targets.

The controller returns 0 migrated processes, preserving process registry state.

This prevents overloading already-troubled nodes during cluster stress.

This represents a degraded cluster scenario requiring manual intervention.

FailoverController coordinates process migration across distributed nodes.

It integrates GlobalProcRegistry for process tracking and NodeDiscovery for health awareness.

When a node fails, all registered processes must migrate to healthy nodes.

The controller iterates through failed-node's processes and reassigns them using transferGlobal().

This implements OTP's distributed process supervision with automatic relocation.

Process migration transfers ownership from source node to target node.

The registry updates the process's node association atomically.

This enables manual rebalancing and targeted process relocation.

Rebalancing automatically redistributes processes after node failure.

The controller selects the first available healthy node as migration target.

This implements OTP's self-healing distributed process supervision.

---
*Generated by [DTR](http://www.dtr.org)*
