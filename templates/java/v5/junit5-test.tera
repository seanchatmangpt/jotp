package {{ package }};

import org.assertj.core.api.WithAssertions;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

{% if comment is defined and comment %}
/**
 * {{ comment }}
 *
 * <p>Generated by ggen from RDF ontology. Do not edit — regenerate with {@code ggen sync}.
 */
{% else %}
/**
 * Unit tests for {@link {{ name }}}.
 *
 * <p>Generated by ggen from RDF ontology. Do not edit — regenerate with {@code ggen sync}.
 */
{% endif %}
@DisplayName("{{ name }}")
class {{ name }}Test implements WithAssertions {

    // ── Construction ─────────────────────────────────────────────────────────

    @Test
    @DisplayName("should be constructed successfully")
    void shouldConstruct() {
        // TODO: instantiate {{ name }} and assert non-null
        assertThat(true).isTrue(); // placeholder
    }

    // ── Equality & hash code ─────────────────────────────────────────────────

    @Test
    @DisplayName("equal instances should have equal hash codes")
    void equalInstancesHaveEqualHashCodes() {
        // TODO: create two equal {{ name }} instances and verify equals/hashCode
        assertThat(true).isTrue(); // placeholder
    }

    // ── toString ─────────────────────────────────────────────────────────────

    @Test
    @DisplayName("toString should include type name")
    void toStringShouldIncludeTypeName() {
        // TODO: verify toString output contains "{{ name }}"
        assertThat("{{ name }}").contains("{{ name }}");
    }
}
