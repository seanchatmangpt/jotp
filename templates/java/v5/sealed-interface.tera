package {{ package }};

{% if comment is defined and comment %}
/**
 * {{ comment }}
 *
 * <p>Generated by ggen from RDF ontology. Do not edit — regenerate with {@code ggen sync}.
 */
{% else %}
/**
 * {{ name }} — Java 26 sealed interface (discriminated union).
 *
 * <p>Generated by ggen from RDF ontology. Do not edit — regenerate with {@code ggen sync}.
 */
{% endif %}
public sealed interface {{ name }}
    permits {{ name }}.Success, {{ name }}.Failure {

    /** Successful variant. */
    record Success(String value) implements {{ name }} {}

    /** Failure variant. */
    record Failure(String reason) implements {{ name }} {}

    // ── Factory methods ──────────────────────────────────────────────────────

    static {{ name }} success(String value) {
        return new Success(value);
    }

    static {{ name }} failure(String reason) {
        return new Failure(reason);
    }

    // ── Pattern-matching helpers ─────────────────────────────────────────────

    default boolean isSuccess() {
        return this instanceof Success;
    }

    default boolean isFailure() {
        return this instanceof Failure;
    }
}
